ci:
  autoupdate_schedule: monthly
  autofix_prs: false

exclude: ^(docs/media/|spreadsheet_conversion/)

repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-json
      - id: check-toml
      - id: check-added-large-files
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.16.2
    hooks:
      # - id: ruff-check
      #   args: ["--fix"]
      - id: ruff-format
        types_or:
          - python
          - pyi
          - jupyter
          - markdown
